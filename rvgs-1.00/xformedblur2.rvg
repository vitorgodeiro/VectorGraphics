local rvg = {}

rvg.scene = scene{
    blur(5.0, {
        fill(circle(100, 100, 80), color.red)
    }):scaled(1/3, 100, 100)
}

rvg.window = window(0,0,200,200)

rvg.viewport = viewport(0,0,600,600)

return rvg
